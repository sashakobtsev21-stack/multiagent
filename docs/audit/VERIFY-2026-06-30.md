# VERIFY-2026-06-30 — независимая верификация состояния сети (по факту, не по докам)

> READ-ONLY. Источник правды — файлы и git (`git ls-files`/`grep`, чтение конфигов), `curl` (best-effort, сеть была доступна). Ничего не менялось, коммитов/пушей нет. Опорные доки (`MASTER-AUDIT-2026-06-24`, `BACKLOG-2026-06-24`, `GSC-baseline-2026-06-28`, `SITES-REGISTRY`, `STANDARDS`) использованы только как «ожидалось», все числа пересчитаны заново.
>
> Сегодня: **2026-06-30**. Клоны: `C:/Users/Oleksandr/Desktop/multiagent/sites/{gruzia,albania,montenegro,croatia,macedonia}-site`.

## Сводная таблица

| # | Пункт | Ожидалось по докам | По факту (git/curl) | Вердикт |
|---|---|---|---|---|
| 1a | Статьи (articles, `.md`/lang) — Грузия | 90 | **91 en / 91 ru / 91 uk** (273 файла, плоско lang/file) | ⚠️ расхождение (+1, реестр устарел) |
| 1b | Статьи — Албания (en-only) | 16 | **25 en** | ⚠️ сильное расхождение (+9) |
| 1c | Статьи — Черногория (en+ru) | 20 | **29 en / 29 ru** | ⚠️ сильное расхождение (+9) |
| 1d | Статьи — Хорватия (en+ru) | 17 | **27 en / 27 ru** | ⚠️ сильное расхождение (+10) |
| 1e | Статьи — Македония (en-only) | 17 | **27 en** | ⚠️ сильное расхождение (+10) |
| 1f | Маршруты (routes/lang) | GE 9 · AL/ME/HR/MK 1 | GE **9** · AL **1** · ME **3** · HR **1** · MK **1** | ⚠️ ME 1→**3** (реестр устарел); прочие сходятся |
| 1g | Рестораны (restaurants/lang) | GE 31 · AL/ME/HR/MK 3 | GE **26** · AL **3** · ME **3** · HR **3** · MK **3** | ⚠️ GE 31→**26** (реестр завышен); прочие сходятся |
| 1h | Коллекция `cities` (пустая у всех 5) | пусто | **пусто везде** (только `.gitkeep`) | ✅ сходится |
| 2 | Живость доменов (HTTP `/`) | все живые (P0 закрыт) | GE/AL/ME/HR/MK = **200 / 200 / 200 / 200 / 200** | ✅ все живы |
| 3 | GSC verify-meta в `BaseLayout` | через **DNS-TXT** (meta опционально, P0-2) | **нет meta** ни в одном `BaseLayout.astro` и нигде в `src/` (×5) | ✅ ожидаемо (DNS-метод; GSC шлёт данные по всем 5) |
| 4a | Черногория `partners.json`: discovercars/getyourguide/booking | P1-2 **открыто** (прямые URL без трекинга) | discovercars=`https://www.discovercars.com/`, getyourguide=`https://www.getyourguide.com/`, booking=`https://www.booking.com/`, все `allowSubId:false` | ❌ **по-прежнему болванки** (P1-2 открыт) |
| 4b | EKTA во всех 5 (`partners.json`) | P1-1 «починить» (backlog: «снято с живых страховых страниц») | **во всех 5**: `ekta` = `https://ektatraveling.com/`, `allowSubId:false` (прямой URL без трекинга) | ❌ **в `/go/`-карте не починен** ни на одном сайте |
| 5 | future-date `publishedAt` (> 2026-06-30) в проде | не должно висеть опубликованного на будущих датах | **AL: 1 шт** — `src/content/routes/en/albania-road-trip-itinerary.md`, `publishedAt: 2026-07-08`, `draft:false`, **live 200**. GE/ME/HR/MK — чисто | ❌ **нарушение** (живой URL с будущей датой) |
| 6 | Рассинхрон календарь↔`src/content` (выборка) | календари актуальны | AL: будущая дата «объяснена» в KALENDAR, но `publishedAt` ≠ реальной дате; ME: 32 маркера публикаций vs 29 en-статей — расхождений нет | ⚠️ см. п.5 (data-level) + ✅ ME ок |

## Детали по находкам

### П.1 — счётчики статей: реестр устарел по всем 5
`git ls-files "src/content/articles/**"` с фильтром `.md/.mdx` и группировкой по языковой подпапке (структура плоская — `articles/<lang>/<file>.md`, проверено: все 273 файла Грузии на глубине 5):

| Сайт | Реестр (статьи/lang) | Факт en | Факт ru | Факт uk |
|---|---|---|---|---|
| Грузия | 90 | 91 | 91 | 91 |
| Албания | 16 | 25 | — | — |
| Черногория | 20 | 29 | 29 | — |
| Хорватия | 17 | 27 | 27 | — |
| Македония | 17 | 27 | — | — |

Языковая политика подтверждается фактом: AL/MK — только `en`; ME/HR — `en+ru` (паритет 1:1); GE — `en+ru+uk` (паритет 1:1). Расхождение — это **отставание `SITES-REGISTRY.md`** (последняя фиксация 2026-06-24; с тех пор шла публикация по календарям), а не пропажа/лишние файлы. Совпадает с открытым в backlog P1-5 (реестр держать по факту).

### П.4a/4b — деньги: болванки и EKTA
- **Черногория P1-2** реально открыт: `montenegro-site/src/data/partners.json` — `discovercars`/`getyourguide`/`booking` всё ещё **прямые URL** домена без SubID (`allowSubId:false`). Клики не атрибутируются. Сам файл это честно фиксирует в `$schema-note` («⚠️ БОЛВАНКИ EN-партнёрок … пока ПРЯМЫЕ URL без атрибуции»).
- **EKTA** во **всех 5** репо — `ekta` указывает на голый `https://ektatraveling.com/` (`allowSubId:false`). Backlog «Статус» утверждает, что P1-1 «снято» — но снято было только из **копий страховых страниц** (`i18n`), а в `/go/`-карте партнёр EKTA остаётся прямым URL без трекинга во всех 5. Т.е. как задача атрибуции дохода EKTA — **не закрыта** (ждёт рабочей трекинг-ссылки Travelpayouts; в note так и написано).

### П.5 — future-date publishedAt (живой URL с будущей датой)
`albania-site/src/content/routes/en/albania-road-trip-itinerary.md`:
- `publishedAt: 2026-07-08`, `updatedAt: 2026-07-08`, `draft: false`.
- Реальные коммиты файла: первый — **2026-06-22** («content(routes): publish Albania Road Trip itinerary»), правка фото — 2026-06-23. То есть страница опубликована 8 дней назад, но датирована на 8 дней вперёд.
- Прод: `https://albaniaguidebook.com/routes/albania-road-trip-itinerary/` → **200** (живая). В `src/pages/**` нет date-gating по `publishedAt`/`new Date` — будущая дата сборку не прячет.
- KALENDAR (строки 50, 142) это состояние **намеренно** закрепляет («✅ ОПУБЛИКОВАН … закреплены на своих датах и НЕ переносятся»). Но это противоречит правилу памяти `dashboard-sync-after-calendar-change` / `qa`-дисциплине: **на будущих датах — только `○`, опубликованное не висит, `publishedAt` = реальная дата публикации.** Корень — `publishedAt` забит будущим слотом календаря, а не датой факта (2026-06-22). Это единственный такой случай в сети (GE/ME/HR/MK — чисто).

### П.6 — календарь↔контент (выборка)
- Албания: единственный явный data-рассинхрон — п.5 (будущая дата у live-маршрута).
- Черногория: `KALENDAR.md` — 32 строки с маркерами публикации (`[x]`/`✅`/`ОПУБЛИКОВАН`) против 29 en-статей; разница объясняется тем, что маркеры включают новости/маршруты/рестораны и заголовки секций — грубого расхождения нет, future-`publishedAt` отсутствует.

## Что НЕ удалось проверить / оговорки
- `curl` отработал по всем 5 (сеть была). Глубокие GSC/Lighthouse/DNS-TXT — вне READ-only объёма, верификация GSC взята из факта «impressions идут» (GSC-baseline) + ручного подтверждения владельца в backlog P0.
- Числа считались по `git ls-files HEAD` (не по рабочему дереву) — отражают закоммиченное состояние `main` каждого клона; `git pull` в рамках аудита не делался, возможен лаг относительно `origin`.
