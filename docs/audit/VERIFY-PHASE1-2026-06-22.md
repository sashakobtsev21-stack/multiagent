# Сверка Этапа 1 — 2026-06-22

Каждый сайт: 1 агент читал отчёт чата репо + 1 агент независимо сверял с реальным состоянием репо через GitHub API (коммиты, файлы, grep на остаточные следы). Владелец запускал промпты дважды.

## Итог: все 5 — verdict `done`, готовы к Этапу 2 (SEO)

| Сайт | Этап 1 | К SEO | Что поймала независимая сверка |
|---|---|---|---|
| Грузия | ✅ | да | pass-1 дал ложную a11y-находку → pass-2 исправил; контент НЕ тронут (верно для эталона) |
| Албания | ✅ | да | pass-1 пропустил `/full-audit`, НЕ заметил грузинский `offline.html` + непомеченную `/go/`-ссылку в проде — pass-2 починил оба |
| Черногория | ✅ | да | созданы `worker/index.ts`+`wrangler.jsonc`+`partners.json` (монетизация `/go/` ожила); `/news` локализован; SETUP очищен |
| Хорватия | ✅ | да | остаточный грузинский след в `SPEC.md` стр.293 («Сарпи/EKTA») — в добивку; использует `goroda` (расходится с др. EN-сайтами) |
| Македония | ✅ | да | `scripts/qa.mjs` всё ещё `TITLE_SUFFIX=' — Georgia Guidebook'` — в добивку; pass-1 оставил баг цен ₾→$ (pass-2 починил) |

Двойной прогон + независимая сверка окупились: поймали то, что чаты сами пропустили (особенно грузинский `offline.html` Албании, который иначе уехал бы в прод).

## Хвосты Этапа 1 (микро-добивка)
- **Хорватия:** `SPEC.md` стр.293 — заменить «EKTA вместо недоступных страховок, сухопутный въезд через Сарпи» на хорватскую фактуру (ЕС/Шенген, евро).
- **Македония:** `scripts/qa.mjs` стр.1 и 22 — `TITLE_SUFFIX`/комментарий → «North Macedonia Guidebook» (иначе проверка длины title некорректна).

## Сквозная инфра-заметка (на Этап 5)
- У всех 5 `daily-news-rebuild.yml` пушит в `main` без QA-гейтов → риск авто-деплоя регрессий.
- У части сайтов `build:covers` вне `build`-пайплайна → адаптивные обложки не генерятся в проде → мобайл качает полноразмерные картинки (бьёт perf). Проверить и встроить в сборку.

## Решение владельца (блокирует Этап 4, влияет на Этап 2)
**Слаги/категории секций.** EN-сайты Албания/Черногория/Македония используют английские слаги (`/cities/`, `/food/`…), Хорватия — `goroda`. Для англоязычного SEO нужны английские слаги везде на 4 EN-сайтах; Грузия остаётся на русско-транслитных. Унифицировать сейчас (контента ~0, дёшево); позже = 301-редиректы.

## Действия владельца (накопились, можно параллельно)
- Домены/GSC: Черногория/Хорватия/Македония — задеплоить + подключить Google Search Console + подать sitemap; подтвердить статус Албании.
- Партнёрки: EKTA через Travelpayouts (трекинг-ссылка с sub_id), DiscoverCars/GetYourGuide(Viator)/Booking — регистрация; до этого клики не атрибутируются.
- Мелочи: Грузия — `plan.pdf` в корне (.gitignore/удалить); dev-CVE (npm audit, только dev-тулинг).
