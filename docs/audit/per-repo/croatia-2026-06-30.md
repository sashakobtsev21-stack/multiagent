# Per-repo deep audit — Croatia Guidebook (croatiaguidebook.com)

**Дата:** 2026-06-30 · **Аудитор:** READ-ONLY (1B) · **Репо:** `sashakobtsev21-stack/croatia-site` · **HEAD:** `7ea4caa`
**Клон:** `C:/Users/Oleksandr/Desktop/multiagent/sites/croatia-site` · **Языки:** en (корень) + ru (`/ru/`); uk убран · **Модель:** en-led + ru (аддитивно)
**Метод:** источник правды — файлы + git + прошлый `dist/`; все числа пересчитаны самостоятельно. curl не выполнялся (сеть best-effort; домен — проверить вручную, но прошлый STAGE-аудит: HTTP 200).

---

## Резюме состояния (факт, не доки)

| Измерение | Факт | Вердикт |
|---|---|---|
| Контент EN/RU | 28/28 articles (паритет), restaurants 3/3, routes 1/1, services 0/0 (пусто), cities-коллекция пустая (.gitkeep) | OK |
| Не-draft материалов | 62 (drafts: 0) | OK |
| Title >60 / Desc >155 (символы) | **0 / 0** | OK |
| Фото-гейт ≥5 | **0 статей под порогом** (3 новости 2–3 фото = норма ≥2); baseline `[]` | ЗАКРЫТ |
| Dedup-гейт ④ | baseline пустой (`reversePairs:[]`, `titlePairs:[]`), live в `npm test`/`qa` | OK |
| CSP / Consent Mode v2 | строгий CSP без `unsafe-inline` в script-src; Consent Mode v2 (default denied → granted) | OK |
| /go/ open-redirect | защищён (https-only target, относительный fallback, encodeURIComponent subId, www→apex 301) | OK |
| Прямые URL партнёрок в теле | 0 (совпадения — только `sources:`-цитаты) | OK |
| Форк-чистота (Georgia/GEL/Сарпи) | 0 реальных (только рус. «грузился»/«грузит») | OK |
| uk-остатки | 0 | OK |

---

## Находки

### [Medium] 💰 Тонкая money-страница: digital-nomad-visa 958 слов (< 1200)
**Доказательство:** `src/content/articles/en/croatia-digital-nomad-visa.md` — 958 слов в теле (единственная не-новостная статья сети Хорватии под порогом 1200; остальные 24 — 1600–1830). Прошлый `dist/` Lighthouse perf этой страницы = 79 (`ROADMAP-FIX.md` M4).
**Влияние:** виза/релокация — высокоинтентный денежный кластер (страховка + eSIM + жильё). 958 слов тоньше нормы STANDARDS (1200–2000) и слабее конкурентов по глубине → потолок по ранжированию.
**Фикс:** дописать до 1200+ (раздел про сроки/продление, налоги-связку с `croatia-taxes-for-foreigners`, требования к доходу 2026, частые отказы); параллельно добор инлайн-фото в бюджете ≤200КБ (закрыть M4). EN+RU одним коммитом (паритет).
**Усилие:** M · **Вердикт:** доработать в Фазе 2 (FIX-LIST).

### [Low] 🔧 HANDOFF «Последний коммит» отстаёт от HEAD
**Доказательство:** `HANDOFF.md:4` — «Последний коммит: `d57ed52` … (+docs-коммит следом)», но HEAD = `7ea4caa` (d57ed52 — предок HEAD на 10 коммитов; тело HANDOFF при этом описывает работу до 30.06 — a11y/B1-B3/de-orphan/ratchet, т.е. ВСЕ коммиты до 7ea4caa). Также «Статей: ~27» при факте 28 EN.
**Влияние:** нарушение правила дисциплины 9 (доки = факт). Тело актуально, врёт только строка-указатель в шапке + округление счётчика. Низкий риск, но именно этот класс ловит память `all-docs-always-current`.
**Фикс:** обновить строку шапки HANDOFF на текущий HEAD + «28 статей» при следующем коммите (правка доков, не контента).
**Усилие:** S · **Вердикт:** поправить попутно.

### [Low] 💰 EKTA — временный прямой URL без атрибуции (сетевой долг)
**Доказательство:** `src/data/partners.json` — `"ekta": { "urlTemplate": "https://ektatraveling.com/", "allowSubId": false }` + комментарий-схема (присланная tpx.gr-ссылка вела на VisitorsCoverage с ошибкой «marker is not subscribed»). `ROADMAP-FIX.md:24` это явно трекает.
**Влияние:** клики на EKTA (релевантно СНГ/UA-аудитории, упомянута в DNV-статье) идут без SubID → доход не атрибутируется. Сам редирект-механизм исправен.
**Фикс:** владельцу — вступить в программу EKTA в Travelpayouts, подставить корректную tpx.gr-ссылку, вернуть `?sub_id={subid}` + `allowSubId:true`. Код менять не нужно (только JSON).
**Усилие:** S (owner-action) · **Вердикт:** держать в backlog партнёрок.

### [Low] 💰 Узкий пул партнёров на страховых страницах (только SafetyWing)
**Доказательство:** `croatia-travel-insurance.md` (EN+RU) и `croatia-digital-nomad-visa.md` дают по 1 страховому офферу — `safetywing` (номад/помесячно). EKTA упомянута текстом, но без рабочего `/go/`-оффера (см. выше). Тэли по сети: trip-tours 28, trip-hotels 22, trip-carhire 16, safetywing 6, kiwitaxi 4, localrent 2, airalo 2 — страховка недопредставлена при доказанном спросе.
**Влияние:** GSC-baseline: `croatia travel insurance` ×5 запросов, 100+ показов, позиции 64–94 = «деньги-возможность №1» по сети. Один оффер (нет single-trip / СНГ-варианта EKTA) недобирает конверсию интента.
**Фикс (аддитивно):** после починки EKTA — добавить 2-й AffiliateBox (EKTA single-trip для СНГ/UA рядом с SafetyWing nomad). Лимит 1–3 бокса соблюдён.
**Усилие:** S · **Вердикт:** связать с EKTA-таском.

### [Low] 🔧 Нет страницы политики конфиденциальности (полный GDPR)
**Доказательство:** `ROADMAP-FIX.md:17` — cookie-баннер (Consent Mode v2) без ссылки на политику. Consent Mode реализован корректно (`public/js/ga-init.js`: default `analytics_storage:'denied'` → granted при «Принять»; `CookieConsent.astro`), но текст баннера не ссылается на privacy-policy.
**Влияние:** механика согласия GDPR-корректна; формально не хватает страницы политики (cookies/GA4/партнёрки) и ссылки из баннера. ЕС/UK = основная аудитория (GSC: US 132, UK 110) → желательно.
**Фикс:** создать `/about/`-подобную страницу политики (EN+RU), сослаться из `CookieConsent`. Низкий приоритет, чисто аддитивно.
**Усилие:** M · **Вердикт:** backlog.

### Подтверждено зелёным (не находки — независимая верификация)
- **Тех-SEO:** `astro.config.mjs` — sitemap i18n (en/ru) + x-default→en + lastmod из updatedAt/publishedAt; filter исключает `/go/` и `relocation/services/` и demo. Прошлый `dist/`: hreflang en↔ru+x-default эмитятся, sitemap-index присутствует, `/go/` и `relocation/services` в карте = 0. `trailingSlash:'always'`, `output:'static'`. `robots.txt` Disallow `/go/` + Sitemap. `_redirects` — полная миграция транслит→EN-слаги (301, оба языка) + `/uk/*`→корень + `/en/*`→корень.
- **Безопасность:** `_headers` — HSTS preload, X-CTO nosniff, Referrer-Policy, Permissions-Policy (вкл. `interest-cohort=()`), X-Frame-Options DENY, CSP с `frame-ancestors 'none'`/`object-src 'none'`/`base-uri 'self'`/`form-action 'self'`, script-src без `unsafe-inline`. qa.mjs статически проверяет CSP + отсутствие inline-обработчиков/`javascript:`.
- **Контент-тон (выборка 3):** `split-nightlife` (EN, 1603w) — живой нейтив, локальная фактура (picigin, špica, Plavac Mali, именованные улицы). `croatia-travel-insurance` (RU, 1830w) — естественный русский без кальки + YMYL-дисклеймер. Не ИИ-генерёнка.
- **Фото:** независимый пересчёт — 24 не-новостные статьи ≥5 изображений; 3 новости 2–3 (норма ≥2); рестораны 3/3 с cover (WARN-беклог фактически пуст). Аудиторский P1-4 «фото-дефицит» — закрыт.
- **a11y/перф (статически):** qa.mjs гейтит JS ≤50КБ/стр, img ≤200КБ, width/height (CLS), alt, h1, render-smoke. a11y-серия из движка портирована (HANDOFF: nav-labels, unique ids, lightbox name, skip-link, тач-таргеты ≥44px).
- **Доки (кроме 2 находок Low выше):** PROGRESS/ROADMAP-FIX/реестр согласованы с git; ROADMAP-FIX честно трекает открытый долг (EKTA, DiscoverCars/GetYourGuide, privacy-policy, M4 perf, L1 npm audit dev-only — 24 уязв. вне прод-бандла).
- **Форк-готовность к /work:** 0 реальных «Georgia/Грузия/GEL/Сарпи» в `.claude/agents/*` и `scripts/*`; категория `cities` (не `goroda`); 28/28 паритет; uk полностью удалён.

---

## Рекомендация 1H (язык / реклама — строго аддитивно)
**Язык: не трогать ru, растить EN.** ru-корпус (28 статей) — естественный живой русский, ранжируемая база (GSC: HR/СНГ-хвост) — НЕ рвать. EN — ведущий и основной источник спроса (US 132 + UK 110 показов). Аддитивно: углублять EN money-кластеры (виза/налоги/страховка), ru держать в паритете тем же коммитом. Новые языки (hr и т.п.) — НЕ вводить.
**Реклама: только аффилиат, дисплей пока НЕ включать.** Приоритет — починить EKTA-атрибуцию и добавить 2-й страховой оффер (EKTA single-trip для СНГ) на insurance/visa-страницы; завести DiscoverCars/GetYourGuide (трекаются в ROADMAP-FIX). Лимит 1–3 `/go/`-бокса соблюдён, прямых URL нет. Дисплей-рекламу — только после роста трафика (рек. владельца: старт без).

## Рекомендация 1I (каденс / KPI — по данным GSC, не по числу статей)
**KPI = деньги/показы/позиции по кластерам, НЕ число статей.** Реперные кластеры из GSC-baseline:
1. **Страховка — приоритет №1:** `croatia travel insurance` ×5, 100+ показов, поз. 64–94. Цель: климб поз. 70 → стр. 1. Действия: углубить insurance-статью (уже 1830w + 4 in/4 out перелинковки — хорошая база), добор свежести/перелинковки с visa/budget/cost-of-living, 2-й оффер. Метрика успеха — позиция и клики по этому запросу, не +N статей.
2. **Виза/релокация:** дотянуть DNV (958→1200+w) + perf 79→90; кластер DNV↔taxes↔open-company↔cost-of-living уже связан.
**Каденс:** сохранить 3 темы/будень (план в `KALENDAR.md` до 31.07, 75 `○`-тем), НО приоритизировать по GSC-сигналу (страховка/виза/деньги) поверх календарной очереди. На будущих датах в плане — только `○`; не плодить тонкие variant-страницы (гейт ④ live). Сверка GSC ~06.07 (как намечено в HANDOFF).
