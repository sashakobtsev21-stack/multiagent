# Усиление конвейера `/work` — verify + план фиксов (2026-06-30)

> Приоритет №1. Verify текущего состояния движка-донора (`sites/gruzia-site`) по 9 точкам надёжности `/work` — **по реальному коду (read-only), не по докам**. Классификация + предлагаемый фикс. Фиксы — в доноре → `qa` зелёный → **порт на все 5 форков**, на «ок» владельца по пунктам.

## Итог
- **ALREADY-OK (2):** #1 `qa` exit-code · #7 crash-банкинг (документирован).
- **PARTIAL (3):** #4 `test:links` · #5 парсер дашборда · #6 ревьюер/preview.
- **NEEDS-FIX (3):** #2 `check-photos` инлайн · #3 `check-dedup` (отсутствует) · #8 лок праймари/резерв.

## По пунктам

**#1. `qa` exit-code — ALREADY-OK.** `scripts/qa.mjs:296` `process.exit(go ? 0 : 1)` (go = `crit.length===0`); строка 294 печатает `ВЕРДИКТ`. Код выхода уже корректен (0=GO, 1=NO-GO). ⚠️ Память `qa-verdict-not-exit-code` расходится с фактом — **сверить точную команду в памяти и обновить** (возможно, речь шла о под-проверке `check`/`test:links`, а не о `qa`). Фикс кода не нужен.

**#2. `check-photos` — NEEDS-FIX.** `scripts/check-photos.mjs` валидирует **только обложку** (`cover.src` + ≤200КБ, стр. 44), инлайн не считает. Денежные страницы проходят `qa=GO` будучи тонкими по фото. Фикс: парсить `<figure>`/`<img>` в теле, требовать **≥5 (статья) / ≥1 (новость) / фото-каждой-остановки (маршрут)**; wired в `npm test`.

**#3. `check-dedup` — NEEDS-FIX (отсутствует).** Есть только `scripts/dup-scan.mjs` (байт-идентичные webp, dev-only, НЕ в `qa`). Анти-каннибализация отсутствует. Фикс: создать гейт — slug-коллизии, reverse-пары транспорта (`a-to-b` vs `b-to-a`), повтор primary-keyword, похожесть title/description; wired в `npm test`. (= гейт 1J.)

**#4. `test:links` — PARTIAL.** `scripts/check-links.mjs` ловит внутренние битые ссылки + `/go/`-валидность, но **НЕ** проверяет `/ru/`/`/uk/`-префиксы на ru/uk-страницах и **НЕ** резолвит кросс-сайт (внешние http пропускает by design). Фикс: проверка языковых префиксов; опц. флаг резолва сестринских доменов (`curl` 200) для кросс-сети.

**#5. Парсер дашборда — PARTIAL.** `grafana/build-status.mjs:94-145` — наивный `includes`-матч даты, нет гарда длинных строк; `published-on-future-date` может сесть в будущую ячейку (ломает семантику «будущее = только ○»). Фикс: гард длины строки, валидация `publishedAt ≤ сегодня` перед показом в будущих ячейках, лог неоднозначных дат.

**#6. `/work` ревьюер/preview — PARTIAL.** SKILL (стр. 49–66): единый писатель + **самопроверка тем же контекстом**, независимого ревьюера нет; авто-публикация при уверенности, принудительного preview для YMYL/новостей нет. Фикс: **(а)** независимый ревьюер-агент (отдельный контекст) на анти-ИИ + факты; **(б)** принудительный owner-preview для YMYL (визы/налоги/страховка) + новостей. ⚠️ (б) снижает автономию — **решение владельца**.

**#7. Crash-банкинг — ALREADY-OK (с оговоркой).** SKILL (стр. 31–35) документирует «банкуй каждый материал сразу» + HANDOFF после сайта. Опц. усиление: pre-push проверка, что весь draft имеет remote tracking перед концом сессии.

**#8. Лок праймари/резерв — NEEDS-FIX (отсутствует).** Нет механизма против одновременного `push` `/work` и Cursor в один репо (только память `one-writer-per-clone`). **Прод-риск конкурентных записей.** Фикс: `.work-lock`-файл или git-тег `work-in-progress-<repo>` + документировать захват/освобождение в `/work` и в резервной (Cursor) схеме.

## Порядок фиксов (рекомендованный)
1. **#8 лок** (прод-риск) → 2. **#3 check-dedup + #2 check-photos** (гейты DoD) → 3. **#4 test:links префиксы** → 4. **#5 парсер дашборда** → 5. **#6a независимый ревьюер** → 6. **#6b YMYL/news preview** (по решению владельца).
Каждый: фикс в доноре → `qa` зелёный → порт на 5 форков. #1/#7 — без фикса (#1: обновить память).
